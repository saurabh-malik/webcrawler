<% if (items.length) { %>
<ul>
<% } %>

    <% items.forEach(function(item){ %>
    <li>
        <a href="<%= item.url %>"><%= item.resource %></a>
        <%- include('siteTree', {items: item.children}); %>
    </li>
    <% }) %>

<% if (items.length) { %>
</ul>
<% } %>